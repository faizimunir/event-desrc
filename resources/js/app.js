import './bootstrap';

// Livewire v3 includes Alpine.js automatically
// If you need to use Alpine.js directly, you can access it via window.Alpine
// But don't call Alpine.start() as Livewire handles that
